this folder help us for manage the files that build the interfece 
ui => Interface
--------------------------------
is conformed for index.html(widgets) --- app.js(logic-front_end) --- style.css(front_end)